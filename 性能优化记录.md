# Oppo 包体射击掉帧 — 性能优化记录

> 日期：2026-08-12
> 问题：Oppo 打包后玩家射击敌人时掉帧，Honor 包体无此问题
> 根因：Oppo 运行在中低端机型上，CPU/GPU 性能较弱，射击链路中存在多处冗余开销在高频射击时累积

---

## 零、本轮新增核心优化 — 对象池 + Ragdoll

> **背景：** 移动端战斗中帧率下降明显，主要原因是大量 `Instantiate()` / `Destroy()` 触发 GC（垃圾回收），以及敌人死亡时 Ragdoll 物理唤醒大量刚体。

### 新增文件

#### GamePool.cs — 通用轻量对象池

**路径：** `Scripts/Utils/GamePool.cs`

基于 `UnityEngine.Pool.ObjectPool<T>` 封装的静态工具类，通过 prefab InstanceID 自动管理不同类型的池。

**API：**
```csharp
GamePool.Spawn(prefab, position, rotation)  // 从池中取出，池空则 Instantiate
GamePool.Despawn(gameObject)                // 回收到池
GamePool.WarmUp(prefab, count)             // 预热（场景初始化时调用）
GamePool.ClearAll()                        // 场景切换时清空
```

**特性：**
- 按 prefab 自动分区，不同类型的子弹/弹孔/特效各自独立池
- `WarmUp()` 预创建对象，避免战斗首次命中时的 Instantiate 卡顿
- `maxSize: 200`，防止内存溢出
- `Despawn()` 自动 fallback 到 `Destroy()`，兼容非池化对象

---

#### PooledLifetime.cs — 池化对象自动回收组件

**路径：** `Scripts/Utils/PooledLifetime.cs`

挂载到池化对象上，到期自动调用 `GamePool.Despawn()` 回收。

```csharp
// 行为：
// OnEnable → 重置计时器 + 重启 ParticleSystem（如果有）
// Update  → 计时，到期自动 Despawn
```

---

### 优化 1：对象池化 — 消除战斗中的 GC 卡顿

| 对象类型 | 原实现 | 优化后 | 影响频率 |
|----------|--------|--------|----------|
| **玩家子弹** | `Instantiate` + `Destroy(lifeTime)` | `GamePool.Spawn` + `GamePool.Despawn` | 每发子弹 (0.14s/发) |
| **敌人子弹** | `Instantiate` + `Destroy(lifeTime)` | `GamePool.Spawn` + `GamePool.Despawn` | 每个敌人射击 |
| **弹孔/命中特效** | `Instantiate` + `Destroy(duration)` | `GamePool.Spawn` + `PooledLifetime` | 每次命中 |
| **受击方向标记 (HitMarker)** | `Instantiate` + `Destroy(1f)` | `GamePool.Spawn` + `PooledLifetime(1f)` | 玩家每次被击中 |
| **敌人溅血特效** | `Stop()` + `Play()` 同一个粒子 | `GamePool.Spawn` + `PooledLifetime` | 每次命中敌人 |

**涉及修改的文件：**

| 文件 | 改动 |
|------|------|
| `AbstractBullet.cs` | 移除 `Awake`+`Destroy`，新增 `Init()` 初始化 + `Despawn()` 回收；弹孔也改为池化 |
| `PlayerWeapon.cs` | `Instantiate` → `GamePool.Spawn` + `Init()` |
| `EnemyWeapon.cs` | `Instantiate` → `GamePool.Spawn` + `Init()` |
| `PlayerHitMarker.cs` | `Instantiate` + `Destroy` → `GamePool.Spawn` + `PooledLifetime` |
| `EnemyBehaviour.cs` | `BloodEffect` (ParticleSystem) → `BloodEffectPrefab` (GameObject)，改为池化 Spawn |
| `ZombieBehaviour.cs` | 同上 |
| `EnemyCreatorEditor.cs` | Creator Tool 不再创建子物体血粒子，直接赋值 prefab 引用 |
| `ZombieCreatorEditor.cs` | 同上 |

**AbstractBullet 核心改造：**
```csharp
// 修改前：Awake 中一次性设置 + Destroy
private void Awake() {
    Destroy(gameObject, lifeTime);  // 每颗子弹都触发延迟 GC
}

// 修改后：Init 初始化 + Despawn 回收
public void Init() { /* 设置初始状态 */ }
protected virtual void Despawn() {
    _isActive = false;
    GamePool.Despawn(gameObject);  // 回收到池，零 GC
}
```

**溅血核心改造：**
```csharp
// 修改前：Stop/Play 同一个粒子（每次命中都重置内部状态）
BloodEffect.Stop();
BloodEffect.Play();

// 修改后：从池中取出新的，旧的自动回收（零 Stop/Play 开销）
GamePool.Spawn(BloodEffectPrefab, bullet.transform.position, Quaternion.identity);
// PooledLifetime 到期后自动 Despawn
```

---

### 优化 2：Ragdoll 物理优化 — 移动端关闭 + 缓存引用

| 问题 | 优化 |
|------|------|
| `GetComponentsInChildren<Rigidbody>()` 在死亡瞬间遍历所有子物体 | `Start()` 中缓存到 `cachedRigidbodies` 数组 |
| 移动端 Ragdoll 物理模拟唤醒大量刚体，CPU 峰值极高 | `#if UNITY_ANDROID \|\| UNITY_IOS` 编译指令强制走死亡动画 |

**涉及修改的文件：**

| 文件 | 改动 |
|------|------|
| `EnemyBehaviour.cs` | `Start()` 缓存 `Rigidbody[] cachedRigidbodies` |
| `EnemyBehaviourState.Death.cs` | 用缓存数组替代 `GetComponentsInChildren`；移动端跳过 Ragdoll |
| `EnemyDamagable.cs` | 懒加载缓存 `GetComponentInParent<EnemyBehaviour>()` |
| `ZombieDamagable.cs` | 懒加载缓存 `GetComponentInParent<ZombieBehaviour>()` |

**Ragdoll 编译分支：**
```csharp
#if UNITY_ANDROID || UNITY_IOS
    // 移动端：跳过 Ragdoll，使用死亡动画
    PlayDeathAnimation();
#else
    if (host.DeathSettings.IsRagdolled)
    {
        foreach (Rigidbody b in host.cachedRigidbodies)
            b.isKinematic = false;
    }
    else { PlayDeathAnimation(); }
#endif
```

---

### 优化 3：子弹碰撞减少 GetComponent 调用

| 文件 | 修改前 | 修改后 |
|------|--------|--------|
| `PlayerBullet.cs` | 每次碰撞 **4 次** GetComponent（Rigidbody×2 + Damagable×2） | **2 次**（直接取值 + null 判断） |
| `EnemyBullet.cs` | 每次碰撞 **2 次** GetComponent（PlayerBehaviour×2） | **1 次** |

```csharp
// 修改前
if (hit.transform.GetComponent<Rigidbody>())
    hit.transform.GetComponent<Rigidbody>().AddForceAtPosition(...);

// 修改后
var rb = hit.transform.GetComponent<Rigidbody>();
if (rb != null) rb.AddForceAtPosition(...);
```

---

### 优化 4：池预热 (WarmUp) — 消除首次命中卡顿

在 `GameManager.Awake()` 中预创建子弹和 HitMarker，避免战斗中首次 Spawn 走 Instantiate：

```csharp
// GameManager.cs — 新增字段
public GameObject[] playerBulletPrefabs;   // AK/Minigun 等多种子弹
public GameObject[] enemyBulletPrefabs;
public GameObject[] hitMarkerPrefabs;

private void WarmUpPools()
{
    for (int i = 0; i < playerBulletPrefabs.Length; i++)
        GamePool.WarmUp(playerBulletPrefabs[i], 30);
    for (int i = 0; i < enemyBulletPrefabs.Length; i++)
        GamePool.WarmUp(enemyBulletPrefabs[i], 30);
    for (int i = 0; i < hitMarkerPrefabs.Length; i++)
        GamePool.WarmUp(hitMarkerPrefabs[i], 5);
}
```

**注意：** 需要在 Unity Inspector 中把对应的子弹 prefab 拖入 GameManager 的数组槽位。

---

---

## 一、早期热修复 — 逐帧/逐次调用优化

> **以下为第一轮优化，主要针对每帧/每次调用的冗余开销。**

### 1. PlayerCrosshair.cs — 🔥 P0 最高优先级

**问题：** `Update()` 每帧调用两次 `GetComponentInParent`（遍历整个父级层级），是最昂贵的每帧调用。

**修改：** 缓存 `FireHitObject` 引用，只在瞄准目标**切换时**才重新调用 `GetComponentInParent`。

```csharp
// 修改前：每帧执行
_player.FireHitObject.GetComponentInParent<EnemyBehaviour>()
_player.FireHitObject.GetComponentInParent<ZombieBehaviour>()

// 修改后：仅在 FireHitObject 引用变化时执行
Transform currentHitObject = _player.FireHitObject;
if (currentHitObject != _cachedFireHitObject)
{
    _cachedFireHitObject = currentHitObject;
    _cachedIsEnemy = currentHitObject != null
      && (currentHitObject.GetComponentInParent<EnemyBehaviour>() != null
        || currentHitObject.GetComponentInParent<ZombieBehaviour>() != null);
}
```

**预期效果：** 每帧省掉 2 次层级遍历，这是最可能的掉帧主因。

---

### 2. AbstractBullet.cs — P1

**问题：** `SpawnDecals()` 中对 `ParticleSystem` 调用了**两次** `GetComponent`（一次判空、一次取值）。

```csharp
// 修改前
float destroyTime = decal.GetComponent<ParticleSystem>()
    ? decal.GetComponent<ParticleSystem>().main.duration
    : decals.destroyTime;

// 修改后：缓存到局部变量
var ps = decal.GetComponent<ParticleSystem>();
float destroyTime = ps != null ? ps.main.duration : decals.destroyTime;
```

---

### 3. EnemyBehaviour.cs — P1

**问题：** `OnBulletHit()` 中血效生成后冗余的 `delay == 1f` 判断。

```csharp
// 修改前
if (ps != null && lifetime.delay == 1f)
    lifetime.delay = ps.main.duration;

// 修改后：直接赋值，去掉多余条件
if (ps != null)
    lifetime.delay = ps.main.duration;
```

---

### 4. ZombieBehaviour.cs — P1

同 EnemyBehaviour.cs，相同修改。

---

### 5. PlayerWeapon.cs — P2

**问题 A：** `Invoke("CanShootNow", ...)` 使用字符串方法名，每次调用触发反射查找。

**修改：** 替换为直接 Coroutine，消除反射开销。

```csharp
// 修改前
Invoke("CanShootNow", ShootFrequency);
Invoke("CanShootNow", 0.4f);

// 修改后
StartCoroutine(CanShootAfterDelay(ShootFrequency));
StartCoroutine(CanShootAfterDelay(0.4f));

// 新增方法
private IEnumerator CanShootAfterDelay(float delay)
{
    yield return new WaitForSeconds(delay);
    CanShootNow();
}
```

**问题 B：** `FireSound.Stop() + Play()` 改为 `PlayOneShot()`。

```csharp
// 修改前
FireSound.Stop();
FireSound.Play();

// 修改后
FireSound.PlayOneShot(FireSound.clip);
```

> `PlayOneShot` 无需 Stop 状态重置，天然支持连发叠加，开销更小。

---

### 6. EnemyWeapon.cs — P2

同 PlayerWeapon.cs，`FireSound` 也改为 `PlayOneShot`：

```csharp
// 修改前
FireSound?.Stop();
FireSound?.Play();

// 修改后
FireSound?.PlayOneShot(FireSound.clip);
```

---

## 三、需要手动检查的事项

### ⚠️ Blood / Decal 预制体是否预挂 PooledLifetime

**这是必须要手动确认的事项。**

在 `EnemyBehaviour.OnBulletHit()`、`ZombieBehaviour.OnBulletHit()` 和 `AbstractBullet.SpawnDecals()` 中都有这样的代码：

```csharp
var lifetime = obj.GetComponent<PooledLifetime>();
if (lifetime == null) lifetime = obj.AddComponent<PooledLifetime>();
```

如果预制体上**没有预挂** `PooledLifetime`，每次命中都会触发 `AddComponent`。
`AddComponent` 是非常昂贵的操作，在 Oppo 低端机上会造成明显瞬时卡顿。

**操作步骤：**
1. 在 Unity 中找到所有 Blood 效果预制体（`BloodEffectPrefab`）
2. 找到所有 Decal 预制体（`BulletDecals.decalProperties.decalPrefabs`）
3. 找到 HitMarker 预制体（`hitMarkerPrefab`）
4. 确认每个预制体上都已经挂了 `PooledLifetime` 组件
5. 如果没有，手动添加并设置合理的 `delay` 值

---

## 四、未修改但值得关注的问题

以下问题未在本轮修改，但如果优化后仍有掉帧，可以进一步处理：

| 优先级 | 问题 | 位置 | 说明 |
|--------|------|------|------|
| P1 | `UpdateFirePoint()` 每帧 `Physics.Raycast(Mathf.Infinity)` | `PlayerBehaviour.Weapon.cs:135` | 即使不射击也在消耗。可限制射程（如 200f）或隔帧执行 |
| P1 | `SpawnDecals` 字符串比较 | `AbstractBullet.cs:69` | `material.name.Contains()` 每次分配字符串。可改用 PhysicMaterial 引用直接比较 |
| P2 | 死亡敌人 `Destroy(host.gameObject, 20f)` 未池化 | `EnemyBehaviourState.Death.cs` | 20 秒延迟 Destroy 敌人本体，多敌人同时死亡时有 GC 压力（溅血已池化） |
| P2 | 敌人生成使用 `Instantiate` | `EnemyGenerator.cs:36` | 未使用对象池，波次生成时有瞬时卡顿 |
| P2 | 手榴弹系统未池化 | `PlayerBehaviour.Grenade.cs:58` | `Instantiate` + `Destroy`，爆炸时多个 Destroy 调用 |

---

## 五、为什么 Honor 没问题但 Oppo 有问题？

最可能的原因：

1. **硬件差异**：Oppo 小游戏用户群以中低端机型为主，CPU 单核性能和 GPU 填充率显著低于 Honor 设备
2. **SDK 开销**：Oppo 小游戏运行时可能有额外的 SDK 开销（广告回调、数据上报等），吃掉部分 CPU 时间片
3. **累积效应**：上述每处单独看开销不大，但在高频射击（0.14s 一发）时累积起来，低端 CPU 扛不住

---

## 六、验证方法

如果优化后仍需进一步排查，可加帧时间监控代码：

```csharp
#if MINIGAME_OPPO
void Update()
{
    float ms = Time.unscaledDeltaTime * 1000f;
    if (ms > 20f)
        Debug.Log($"[PERF] Frame slow: {ms:F1}ms");
}
#endif
```

打包后用 `adb logcat | grep PERF` 看输出，确认掉帧是否消除。

---

## 七、全部修改文件总览

### 新增文件
| 文件 | 说明 |
|------|------|
| `Scripts/Utils/GamePool.cs` | 通用轻量对象池（Spawn/Despawn/WarmUp/ClearAll） |
| `Scripts/Utils/PooledLifetime.cs` | 池化对象自动回收组件（到期 Despawn + 重启 ParticleSystem） |

### 已修改文件
| 文件 | 优化内容 |
|------|----------|
| `Scripts/Entities/Bullet/AbstractBullet.cs` | 对象池化（Init/Despawn）、弹孔池化、GetComponent 缓存 |
| `Scripts/Entities/Bullet/PlayerBullet.cs` | GetComponent 从 4 次减到 2 次 |
| `Scripts/Entities/Bullet/EnemyBullet.cs` | GetComponent 从 2 次减到 1 次 |
| `Scripts/Entities/Player/Weapon/PlayerWeapon.cs` | 子弹 Instantiate → GamePool.Spawn |
| `Scripts/Entities/EnemySoldier/EnemyWeapon.cs` | 子弹 Instantiate → GamePool.Spawn |
| `Scripts/Entities/EnemySoldier/EnemyBehaviour.cs` | 缓存 Rigidbody[]、BloodEffectPrefab 池化溅血 |
| `Scripts/Entities/EnemySoldier/EnemyBehaviourState.Death.cs` | 缓存 Rigidbody、移动端跳过 Ragdoll、移除手动 BloodEffect Destroy |
| `Scripts/Entities/EnemySoldier/EnemyDamagable.cs` | 懒加载缓存 GetComponentInParent |
| `Scripts/Entities/EnemyZombie/ZombieBehaviour.cs` | bloodEffectPrefab 池化溅血 |
| `Scripts/Entities/EnemyZombie/ZombieBehaviourState.Death.cs` | 移除手动 bloodEffect Destroy |
| `Scripts/Entities/EnemyZombie/ZombieDamagable.cs` | 懒加载缓存 GetComponentInParent |
| `Scripts/UI/Player/Misc/PlayerHitMarker.cs` | HitMarker Instantiate → GamePool.Spawn |
| `Scripts/Entities/GameManager/GameManager.cs` | 新增 WarmUp 预热（子弹/HitMarker 数组） |
| `Editor/CreatorTools/EnemyCreatorEditor.cs` | BloodEffect → BloodEffectPrefab 赋值方式 |
| `Editor/CreatorTools/ZombieCreatorEditor.cs` | bloodEffect → bloodEffectPrefab 赋值方式 |
| `Scripts/UI/Player/Misc/PlayerCrosshair.cs` | GetComponentInParent 缓存（早期优化） |
| `Scripts/Entities/Player/Weapon/PlayerWeapon.cs` | Invoke → Coroutine、PlayOneShot（早期优化） |
| `Scripts/Entities/EnemySoldier/EnemyWeapon.cs` | PlayOneShot（早期优化） |
